output/
node_modules/
dist/
release/
*.log
.DS_Store
*.tsbuildinfo

# Generated by scripts/make-icon.mjs (root copy only; the tracked favicon
# lives at src/renderer/public/tool_icon.ico)
/tool_icon.ico

# Legacy Electron build artifacts (no longer produced; ignored in case they linger)
out/

# Superpowers working docs (plans/specs) — local only, not part of the build
docs/superpowers/

# Tauri build artifacts
src-tauri/target/
src-tauri/gen/schemas/
src-tauri/output/

# Tauri updater signing key — NEVER commit. The private key lives only here
# locally and as the TAURI_SIGNING_PRIVATE_KEY GitHub Actions secret.
src-tauri/.tauri/

# Playwright MCP session artifacts (snapshots, console logs)
.playwright-mcp/

# Impeccable plugin local cache
.impeccable/
